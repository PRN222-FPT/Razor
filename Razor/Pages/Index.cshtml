@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">GROUP1 Ass2</h1>
    <p class="lead">Razor Pages presentation layer with ServiceLayer, DataAccessLayer, Repository, and Unit of Work.</p>
    <p>
        <a asp-page="/Categories/Index" class="btn btn-primary">Manage Categories</a>
        <a asp-page="/Products/Index" class="btn btn-outline-primary">Manage Products</a>
    </p>
</div>
