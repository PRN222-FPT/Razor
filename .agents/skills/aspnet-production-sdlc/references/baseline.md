# ASP.NET Baseline References

- Use `AGENTS.md` for canonical project rules.
- Use `ARCHITECTURE.md` for the current Razor Pages layered architecture.
- Use Microsoft ASP.NET Core security guidance for authentication, authorization, antiforgery, XSS, SQL injection, HTTPS, and secrets.
- Use Microsoft ASP.NET Core best practices for async hot paths, paging, caching, and exception handling.
- Use NIST SSDF and OWASP SAMM as SDLC and secure software maturity baselines.
